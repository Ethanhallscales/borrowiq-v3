/**
 * Configurable rate benchmarks — update this file when rates change.
 * Used by Path C (refinance) only. Do not reference from Path A or B.
 */

/** Best available refinance comparison rate shown to Path C users. */
export const BEST_AVAILABLE_RATE = 0.0585; // 5.85% — update as market moves

/** Human-readable label for the best available rate. */
export const BEST_AVAILABLE_RATE_LABEL = "Best available rate";
